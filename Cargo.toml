[package]
name = "duckstream"
version = "0.1.0"
edition = "2021"
description = "A DuckDB extension for querying NATS JetStream, including live/tail streaming"
license = "MIT"
repository = "https://github.com/ike/duckstream"

[lib]
crate-type = ["cdylib"]

[dependencies]
